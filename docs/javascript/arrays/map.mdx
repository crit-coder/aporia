import CodeBlock from '@theme/CodeBlock';
import mapUsersCode from '@site/docs/javascript/arrays/examples/map-examples';

---

id: map
title: Array.prototype.map()

---

# Array.prototype.map()

The `map()` method creates a new array by applying a function to each element of the original array.

---

## 🔍 Description

- 🔁 Does **not** mutate the original array
- ✅ Returns a **new array**
- 🎯 Takes a callback function with `(value, index, array)`

---

## 🧪 Syntax

```js
array.map((value, index, array) => {
  // return transformed value
});
```

## Code examples

<details>
  <summary>
    Show TypeScript Example: <code>map-examples.js</code>
  </summary>
  <CodeBlock language="js" title="map-examples.js">
    {mapUsersCode}
  </CodeBlock>
</details>
